<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Showkase component browser (debug builds only, separate launcher icon) -->
        <activity
                android:name=".ui.catalog.ShowkaseLauncherActivity"
                android:exported="true"
                android:label="Viecz Showkase">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
