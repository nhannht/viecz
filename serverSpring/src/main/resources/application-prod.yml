# Production Profile Configuration

spring:
  jpa:
    show-sql: false

logging:
  level:
    com.viecz.server: INFO
    org.springframework.web: WARN
    org.springframework.security: WARN

server:
  error:
    include-message: never
    include-stacktrace: never
